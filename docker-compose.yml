version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: personal-website
    ports:
      - "2000:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
